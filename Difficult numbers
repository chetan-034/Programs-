#include <iostream>
#include <cmath>
using namespace std;

// Function to check if a number is prime
bool isPrime(int n) {
    if (n <= 1) return false;
    if (n == 2) return true;
    if (n % 2 == 0) return false;

    for (int i = 3; i <= sqrt(n); i += 2) {
        if (n % i == 0) return false;
    }
    return true;
}

// Function to count “difficult numbers” (composite numbers)
int countDifficultNumbers(int low, int high) {
    int count = 0;
    for (int i = low; i <= high; i++) {
        if (!isPrime(i) && i > 1) {
            count++;
        }
    }
    return count;
}

int main() {
    int low, high;
    cout << "Enter the range (low high): ";
    cin >> low >> high;

    int result = countDifficultNumbers(low, high);
    cout << "Count of difficult numbers: " << result << endl;

    return 0;
}
