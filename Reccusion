#include <iostream>
using namespace std;

void printNumbers(int n) {
    // Base case
    if (n == 0)
        return;

    // Recursive call
    printNumbers(n - 1);

    // Work after recursion
    cout << n << " ";
}

int main() {
    int n = 5;
    printNumbers(n);
    return 0;
}
